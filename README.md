# WebPartyGames

Instant browser party games. No logins, just a link.

## Local dev

Install dependencies from the repo root, then run the dev server:

- `npm install`
- `npm run dev`

The Next.js app lives in `apps/webpartygames`.


